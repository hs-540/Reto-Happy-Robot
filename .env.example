# Copy this file to the repository root as `.env` and fill in the values.
# The backend loads `.env` from the repo root on startup; `.env` is outside
# version control and must never be committed to git.
#
# Every variable without a default here is required: `config.ts` validates the
# whole file on boot and exits listing what failed (never the value). An empty
# string counts as missing.

# HTTP port of the backend
PORT=3001

# Decision engine cadence in ms. The demo compresses 30 crisis-minutes into
# ~2 real minutes (TIME_SCALE = 15 in backend/src/sim.ts), so keep this at a
# couple of seconds: each idle tick would otherwise let a minute of crisis pass.
TICK_MS=2000

# ── LLM provider: Helmcode (OpenAI-compatible interface) ─────────────
# The provider is configuration, not code: switching it means editing these
# variables, without touching `config.ts`. If the provider goes down, the
# engine degrades to the deterministic rules fallback and the simulation
# does not stop.
LLM_BASE_URL=
LLM_API_KEY=
LLM_MODEL=

# Embedding model for the RAG, via the same provider
LLM_EMBEDDING_MODEL=text-embedding-3-small

# Provider label; only shows up in logs
LLM_PROVIDER=helmcode

# ── HappyRobot: real voice calls and messages ────────────────────────
# The integration is built and wired end to end: the agent dials through
# `POST {HAPPYROBOT_BASE_URL}/api/v1/dial/outbound` and the hang-up comes back
# to `POST /api/call/outcome`, where a refusal or a delay forces a re-plan.
#
# This variable is REQUIRED and must not be left empty: an empty string fails
# validation and the backend exits. With a real key the calls go out for real.
# Without one, set it to the single placeholder token that `credentialUsable()`
# in `backend/src/happyrobot.ts` recognises as "no credential" — that selects
# the simulated client, which exercises the same chain with scripted answers so
# the demo stands up. Any OTHER non-empty value is taken as a real key and every
# call will fail authentication.
HAPPYROBOT_BASE_URL=https://app.happyrobot.ai
HAPPYROBOT_API_KEY=

# ── Local RAG (Chroma) ───────────────────────────────────────────────
# Port of the local Chroma server. If one is already on that port it gets reused.
CHROMA_PORT=8000

# Chroma local data, path relative to the repo root
CHROMA_PATH=backend/chroma-data
